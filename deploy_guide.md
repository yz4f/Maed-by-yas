# 🚀 دليل استضافة وتشغيل متجر تـعـن والبوت الموحد

لقد قمنا بدمج **بوت الديسكورد** و**الموقع الإلكتروني** في مشروع واحد متكامل وموحد لتسهيل الإدارة، ومشاركة قاعدة البيانات بشكل فوري وتلقائي.

---

## 📋 الخيارات المتاحة للاستضافة

بما أن المشروع يحتوي على **بوت ديسكورد نشط (WebSocket)** و**قاعدة بيانات محلية (JSON)**، فإن الاستضافة تنقسم إلى خيارين:

### الخيار الأول: الاستضافة المتكاملة 24/7 (موصى به للغاية)
**المنصات المقترحة:** [Render.com](https://render.com) أو [Railway.app](https://railway.app) أو **سيرفر خاص VPS**
*   **المميزات:**
    *   يعمل بوت الديسكورد 24 ساعة بدون انقطاع.
    *   حفظ تلقائي ودائم لقاعدة البيانات (الأكواد المستعملة، سجلات الأعضاء).
    *   تشغيل الموقع والبوت معاً بضغطة زر واحدة.

### الخيار الثاني: استضافة الموقع على Vercel والبوت بشكل منفصل
*   **المميزات:** سرعة فائقة للموقع على خوادم Vercel العالمية.
*   **العيوب:** 
    *   خوادم Vercel هي خوادم مؤقتة (Serverless)، وبالتالي لا تدعم تشغيل بوت الديسكورد مباشرة (لأن البوت يتطلب اتصالاً مستمراً بالإنترنت).
    *   قاعدة البيانات المكتوبة في ملفات JSON ستكون مؤقتة وتضيع بمجرد إعادة تشغيل الخادم.

---

## 🛠️ طريقة الاستضافة المتكاملة على Render أو Railway

لتشغيل الموقع والبوت معاً بشكل دائم:

1.  **رفع المشروع على GitHub:**
    *   قم بإنشاء مستودع (Repository) جديد خاص (Private) على حسابك في GitHub.
    *   ارفع ملفات المشروع إليه.

2.  **التسجيل في منصة الاستضافة (مثال: Render.com):**
    *   أنشئ حساباً واربطه بـ GitHub.
    *   اضغط على **New +** ثم اختر **Web Service**.
    *   اختر المستودع الخاص بالمتجر.

3.  **إعدادات التشغيل:**
    *   **Runtime:** `Node`
    *   **Build Command:** `npm run build`
    *   **Start Command:** `npm start`

4.  **إضافة المتغيرات البيئية (Environment Variables):**
    أضف المتغيرات التالية في قسم `Environment`:
    *   `DISCORD_BOT_TOKEN`: توكن البوت الخاص بك.
    *   `DISCORD_CLIENT_ID`: معرف تطبيق الديسكورد.
    *   `DISCORD_CLIENT_SECRET`: السر الخاص بالتطبيق.
    *   `DISCORD_GUILD_ID`: معرف السيرفر (Guild ID).
    *   `APP_URL`: رابط موقعك بعد الاستضافة (مثال: `https://ta3n-store.onrender.com`).

---

## 🌐 طريقة استضافة الموقع فقط على Vercel

إذا كنت ترغب في رفع الموقع على Vercel:

1.  ربط حساب GitHub بمنصة [Vercel.com](https://vercel.com).
2.  اختر مشروع المتجر واضغط **Deploy**.
3.  سيتكفل ملف `vercel.json` الذي قمنا بإنشائه بتوجيه الطلبات وبناء الواجهة الأمامية والخلفية تلقائياً.
4.  أضف المتغيرات البيئية في إعدادات المشروع على Vercel.

---

## ⚙️ التوكن والمعلومات المطلوبة في ملف `.env`

تأكد من إعداد الملف البيئي `.env` بالمعلومات الصحيحة لضمان عمل البوت والموقع:

```env
PORT=3000
DISCORD_BOT_TOKEN=توكن_البوت_الخاص_بك
DISCORD_CLIENT_ID=معرف_التطبيق
DISCORD_CLIENT_SECRET=سر_التطبيق
DISCORD_GUILD_ID=معرف_السيرفر
APP_URL=رابط_الموقع_الخاص_بك
```
